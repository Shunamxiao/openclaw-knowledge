# OpenClaw 多 Agent 配置文档

## 📊 架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                      OpenClaw Gateway                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────┐          ┌─────────────────┐           │
│  │  💼 main        │          │  📡 monitor     │           │
│  │  小秘书         │          │  资讯监控员      │           │
│  │                 │          │                 │           │
│  │  Model:         │          │  Model:         │           │
│  │  Claude Opus    │          │  Kimi K2.5      │           │
│  │                 │          │                 │           │
│  │  Workspace:     │          │  Workspace:     │           │
│  │  /workspace     │          │  /workspace-    │           │
│  │                 │          │  monitor        │           │
│  └────────┬────────┘          └────────┬────────┘           │
│           │                            │                     │
│           ▼                            ▼                     │
│  ┌─────────────────┐          ┌─────────────────┐           │
│  │ 私聊 / WebChat  │          │ 群组            │           │
│  │ 默认路由        │          │ -1003312777824  │           │
│  └─────────────────┘          └─────────────────┘           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🤖 Agent 定义

### main (默认 Agent)

```json
{
  "id": "main",
  "identity": {
    "name": "小秘书 (Little Secretary)",
    "emoji": "💼"
  },
  "workspace": "/root/.openclaw/workspace",
  "model": {
    "primary": "anthropic/claude-opus-4-5-20251101",
    "fallbacks": ["kimi-coding/k2p5", "qwen-portal/coder-model"]
  }
}
```

**职责：**
- 日常对话、任务处理
- 复杂问题分析
- 系统管理

---

### monitor (资讯监控 Agent)

```json
{
  "id": "monitor",
  "name": "资讯监控员",
  "workspace": "/root/.openclaw/workspace-monitor",
  "agentDir": "/root/.openclaw/agents/monitor/agent",
  "model": {
    "primary": "kimi-coding/k2p5",
    "fallbacks": ["qwen-portal/coder-model"]
  },
  "identity": {
    "name": "资讯监控员",
    "emoji": "📡"
  }
}
```

**职责：**
- 4Gamer 日本手游资讯监控
- AppMedia 新游监控
- Gamebase 台湾游戏新闻监控

---

## 🔗 群组绑定 (bindings)

```json
{
  "agentId": "monitor",
  "match": {
    "channel": "telegram",
    "peer": {
      "kind": "group",
      "id": "-1003312777824"
    }
  }
}
```

**说明：** 群组 `-1003312777824` 的所有消息路由到 `monitor` Agent

---

## 💬 群组配置 (channels.telegram.groups)

```json
{
  "-1003312777824": {
    "enabled": true,
    "requireMention": false,
    "systemPrompt": "你是 资讯监控员 📡，专门负责游戏资讯的自动化监控。\n\n## 核心职责\n- 执行 4Gamer 日本手游资讯监控\n- 执行 AppMedia 新游监控\n- 执行 Gamebase 台湾游戏新闻监控\n- 将监控结果推送到本群组\n\n## 工作流程\n1. 收到定时任务 → 发送开始通知\n2. 执行监控脚本 → 获取数据\n3. 整理结果 → 发送到群组\n\n## 输出风格\n- 使用 emoji 标记状态\n- 简洁明了，不啰嗦\n- 重要信息突出显示\n\n## 脚本位置\n/root/.openclaw/workspace/skills/web_scraper/"
  }
}
```

---

## ⏰ 定时任务

| 任务 | Agent | 时间 | 脚本 |
|------|-------|------|------|
| 4Gamer监控 | monitor | `*/30 * * * *` | `monitor_4gamer_translated.py` |
| AppMedia监控 | monitor | `0 8-23 * * 1-5` | `appmedia_monitor_translated.py` |
| Gamebase监控 | monitor | `0 10-23 * * 1-5` | `gamebase_smart_monitor.py` |
| 天气推送 | main | `0 8 * * *` | `send_weather_free.py` |
| 用量报告 | main | `0 19 * * *` | systemEvent |
| 知识推送 | main | `0 0 * * *` | systemEvent |
| 自动更新 | main | `0 3 * * *` | `check_update.py` |

---

## 📁 工作空间结构

```
/root/.openclaw/
├── workspace/                    # main Agent 工作空间
│   ├── AGENTS.md
│   ├── SOUL.md
│   ├── IDENTITY.md
│   ├── USER.md
│   ├── MEMORY.md
│   ├── memory/
│   └── skills/
│       ├── web_scraper/          # 监控脚本
│       ├── translation/          # 翻译模块
│       ├── notification/         # 通知模块
│       └── email/                # 邮件模块
│
├── workspace-monitor/            # monitor Agent 工作空间
│   ├── AGENTS.md
│   ├── SOUL.md
│   └── IDENTITY.md
│
└── agents/
    └── monitor/
        └── agent/                # monitor Agent 状态目录
```

---

## 🔧 扩展新 Agent 模板

### 1. 创建 Agent

```bash
openclaw agents add <agent_id> \
  --workspace /root/.openclaw/workspace-<name> \
  --model <model_id> \
  --bind "telegram:<group_id>" \
  --non-interactive
```

### 2. 设置身份

```bash
openclaw agents set-identity --agent <agent_id> --name "名称" --emoji "🎯"
```

### 3. 添加群组配置

```json
// channels.telegram.groups
{
  "<group_id>": {
    "enabled": true,
    "requireMention": false,
    "systemPrompt": "你是 XXX Agent..."
  }
}
```

### 4. 添加绑定

```json
// bindings
{
  "agentId": "<agent_id>",
  "match": {
    "channel": "telegram",
    "peer": {
      "kind": "group",
      "id": "<group_id>"
    }
  }
}
```

### 5. 创建工作空间文件

- `IDENTITY.md` - 身份定义
- `AGENTS.md` - 工作指南
- `SOUL.md` - 性格设定

---

## 📝 配置字段说明

| 字段 | 说明 |
|------|------|
| `agents.list` | Agent 列表定义 |
| `bindings` | 消息路由绑定 |
| `channels.telegram.groups` | 群组特定配置 |
| `model.primary` | 主模型 |
| `model.fallbacks` | 备用模型列表 |
| `requireMention` | 是否需要 @提及 |
| `systemPrompt` | 群组专属系统提示词 |

---

*文档更新时间: 2026-02-05*
